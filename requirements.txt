# Runtime dependencies. This file — and only this file — is installed into the
# production image (see Dockerfile). Linters, test runners and fixtures belong
# in requirements-dev.txt: shipping them enlarges the image and its CVE surface.

Django==5.1.15
psycopg2-binary==2.9.10
python-dateutil==2.9.0.post0
python-dotenv==1.1.0
djangorestframework==3.16.0
openai==2.41.0

# HU27 — Exportación de calificaciones y asistencia a Excel y PDF
openpyxl>=3.1.0
reportlab>=4.0.0

# HU30 — Deployment (Docker/Compose, WSGI server, static, cache, DB URL)
gunicorn==23.0.0
whitenoise==6.8.2
django-redis==5.4.0
dj-database-url==2.3.0
